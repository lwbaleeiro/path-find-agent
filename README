# Pathfinder

**Pathfinder** will navigate through obstacles, aiming to reach the destination on the other side of the screen. With the power of AI, Pathfinder will learn and adapt to the environment, avoiding obstacles and finding the optimal path. 🎮🤖

## How to Execute

- First, you need to install the requirements found in the `requirements.txt` file:

  ```bash
  pip install -r requirements.txt

- Then, simply execute the `game.py` file in the project folde:

  ```bash
    cd your/folder/path/path-find-agent
    python game.py